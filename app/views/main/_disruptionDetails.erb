<h1>Route <%= @disruption.route %> disrupted <%= @disruption.getRunString.downcase %> - detected
    at  <%= @disruption.getDetectedAt %></h1>
<div class="row">
    <div class="large-12 columns">
<span class="info"> Route disrupted by <strong><%= @disruption.getDelay %></strong> minutes
    between
    <a href="http://countdown.tfl.gov.uk/#|searchTerm=<%= @disruption.fromStop.code %>" target="_blank"><%= @disruption.fromStop.name.split.map(&:capitalize).join(' ') %></a>
    and
    <a href="http://countdown.tfl.gov.uk/#|searchTerm=<%= @disruption.toStop.code %>" target="_blank"><%= @disruption.toStop.name.split.map(&:capitalize).join(' ') %></a>
    <br>
    Total average disruption observed across route <strong><%= @disruption.getTotalDelay %></strong> minutes
</span>
    </div>
</div>
<div class="row">
    <div class="large-8 columns">
        <%= image_tag @chart %>
    </div>
    <div class="large-4 columns">
        <p>
            <% @sections.each do |section| %>
                <% if (@xaxisLabels.include? section.startStopLBSLCode) %>
                    <%= section.startStopLBSLCode %>  -
                    <a href="http://countdown.tfl.gov.uk/#|searchTerm=<%= section.startStop.code %>" target="_blank"><%= section.startStop.name.split.map(&:capitalize).join(' ') %></a>
                    - lost <strong><%= section.latestLostTime.lostTimeInSeconds / 60 %></strong> minutes
                    <br>
                <% end %>
            <% end %>

        </p>
    </div>
</div>

<!--<a href="<= @chart %>">chart</a> -->





<!--
<p>
    <i>More detailed information about the selected disruption can be displayed here. Some examples below: </i>
</p>
<ul>
    <li>Other buses that experience disruption in the same area.</li>
    <li>Graphic showing the delay severity changing in time.</li>
    <li>Each section delay.</li>
    <li>Number of buses logged on this route in the past 2h.</li>
    <li>CentreComm operator comments/notes.</li>
</ul>
<= image_tag "SampleMapOverview.png" %>
<!--<img src="/images/SampleStripMap.png" alt="Map" align="middle">-->
<a class="close-reveal-modal">&#215;</a>


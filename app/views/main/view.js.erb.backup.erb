if (document.URL == "http://localhost:3000/main/index" || document.URL == "http://localhost:3000/") {
if (<%= @update%>) {
$("#disruptionList").html("<%= raw escape_javascript(render(:partial => 'disruptionList')) %>")
}
$("#lastUpdateTime").html("<%= Time.new.strftime("%m/%d/%Y %H:%M:%S ")%>")

setTimeout(
function () {
new $.ajax('/main/view', {
method: 'get'
});
},
10000
);
}

